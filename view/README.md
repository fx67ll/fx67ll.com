# Theme views

Each child directory is a standalone static project with its own `index.html` and local assets.

- `default`: current production assets copied from `dist`
- `front`: front-end resume theme
- `bigdata`: big-data resume theme
- `2026`: 2026 resume theme

Open any theme's `index.html` directly, or serve that theme directory with a static HTTP server.
